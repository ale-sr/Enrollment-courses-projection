import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Open Sans",
    body: "Encode Sans",
  },
  colors: {
    utecBlue: "#52C0F5",
  },
});

export default theme;
